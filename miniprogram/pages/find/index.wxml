<view class="newList">
  <view class="item" wx:for="{{ list }}" wx:key="index" bindtap="openDetail" data-index="{{ index }}">
    <image class="banner" src="{{ item.banner }}" mode="aspectFill"></image>
    <view class="title">{{ item.title }}</view>
  </view>
</view>